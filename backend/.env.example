# MongoDB 連線字串。本機可用 mongodb://127.0.0.1:27017/material_editor
# 在 Zeabur 上由 MongoDB 服務注入(綁定變數即可)
MONGODB_URI=mongodb://127.0.0.1:27017/material_editor

# API 監聽埠(Zeabur 會注入 PORT)
PORT=4000

# 允許跨來源的前端網址(CORS)。多個用逗號分隔。留空 / 設為 * 代表全部允許
FRONTEND_ORIGIN=http://localhost:5173
